[gd_resource type="Resource" script_class="CharacterData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/character_data.gd" id="1_character_data"]

[resource]
script = ExtResource("1_character_data")
id = "rogue"
display_name = "Rogue"
description = "Fastest thing on the map. Circles behind the boss, hits hard, and leaves before anything lands on him."
accent_color = Color(0.45, 0.82, 0.55, 1)
model_scene = null
model_scale = 1.0
model_offset = Vector3(0, 0, 0)
model_yaw_offset_deg = 0.0
head_height = 1.5
anim_idle = "idle"
anim_walk = "walk"
anim_run = "run"
anim_jump = "jump"
anim_fall = "fall"
max_health = 100
walk_speed = 5.0
run_speed = 9.2
jump_velocity = 7.6
attack_damage = 17
